import { DecisionItem, AutoCompletedTask, MemoryItem, DocumentItem, MeetingItem, EmailItem } from './types';

export const initialDecisions: DecisionItem[] = [];
export const initialAutoCompletedTasks: AutoCompletedTask[] = [];
export const initialMemoryBank: MemoryItem[] = [];
export const initialDocuments: DocumentItem[] = [];
export const initialMeetings: MeetingItem[] = [];
export const initialEmails: EmailItem[] = [];
