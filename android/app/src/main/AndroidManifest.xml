<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network Access -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Contextual System Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Foreground Service & Battery Exemption for Overnight Autonomy -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Package Visibility Queries for On-Device Price Comparison & Automation -->
    <queries>
        <!-- Food Delivery -->
        <package android:name="com.application.zomato" />
        <package android:name="in.swiggy.android" />
        <!-- E-Commerce & Fashion -->
        <package android:name="com.flipkart.android" />
        <package android:name="com.flipkart.shopsy" />
        <package android:name="in.amazon.mShop.android.shopping" />
        <package android:name="com.amazon.mShop.android.shopping" />
        <package android:name="com.myntra.android" />
        <package android:name="com.meesho.supply" />
        <package android:name="com.ril.ajio" />
        <package android:name="com.purplle.purplle" />
        <!-- Quick Commerce -->
        <package android:name="com.grofers.customerapp" />
        <package android:name="com.zeptonow.android" />
    </queries>

    <application
        android:name=".ContrilApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Contril"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Native Jetpack Compose MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Android App Links -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="contril.app" />
                <data android:pathPrefix="/command" />
            </intent-filter>

            <!-- Native AppAuth RFC 7636 PKCE Redirect: com.contril.app.debug:/oauth2redirect -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.contril.app.debug"
                    android:path="/oauth2redirect" />
            </intent-filter>

            <!-- Official Contril OAuth Android Deep Link: contril://auth/callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="contril"
                    android:host="auth"
                    android:path="/callback" />
            </intent-filter>

            <!-- Netlify Web-to-App Bridge Intent Filter -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="contril.netlify.app" />
            </intent-filter>

            <!-- Fallback Deep Link Filter -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="contril"
                    android:host="login-callback" />
            </intent-filter>
        </activity>

        <!-- On-Device Price Comparison Accessibility Service -->
        <service
            android:name=".data.automation.ContrilAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/contril_accessibility_service_config" />
        </service>

        <!-- Overnight Autonomy Monitoring Foreground Service -->
        <service
            android:name=".service.OvernightAutonomyService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

    </application>

</manifest>
